import { createTheme } from "@mui/material";
export const theme = createTheme({
  direction: "rtl",
  typography: {
    fontFamily: "tanha, vazir, roboto",
  },
  palette: {
    mode: "dark",
    primary: {
      main: "#8be9fd",
    },
    secondary: {
      main: "#bd93f9",
    },
  },
});
